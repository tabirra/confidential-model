__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Secrets and generated artifacts — never commit these
secrets/
*.b64
*.tar.gz
*.tar.gz.enc
/tmp-confidential-model/
.env
notas.txt

# KBS admin credentials (Layer 3) — never commit these
kbs/*.key
kbs/kbs-admin*
